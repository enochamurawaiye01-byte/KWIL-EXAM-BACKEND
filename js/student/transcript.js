/* ============================================
   STUDENT TRANSCRIPT — BACKEND PENDING
   No student-facing transcript endpoint exists yet.
   Renders the sample row already in the HTML.
   ============================================ */

auth.requireStudent();

document.addEventListener("DOMContentLoaded", () => {
  renderStudentNavbar("transcript");
});
